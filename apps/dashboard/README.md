# Operator dashboard

Dependency-free dashboard starter. It polls the local edge API at `http://localhost:3001/v1/farm-state` every ten seconds.

Run `npm run dev:dashboard` from the repository root.

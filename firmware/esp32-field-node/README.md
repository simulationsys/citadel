# ESP32 field node

Reads soil-moisture and water-level analog inputs and prints a shared JSON payload at 10-second intervals. The relay starts in the safe OFF state.

Add DHT/BME280, rain gauge, Wi-Fi, and HTTP/MQTT transport incrementally. Never commit `config.h`.
